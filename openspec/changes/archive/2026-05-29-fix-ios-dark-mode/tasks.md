## 1. HTML Meta Tag

- [x] 1.1 Add `<meta name="color-scheme" content="dark">` inside `<head>` in `index.html`

## 2. CSS Declaration

- [x] 2.1 Add `color-scheme: dark` property to the `:root` block in `src/style.css`

## 3. Verification

- [ ] 3.1 Confirm that color swatches render with unmodified colors on iOS Safari (dark mode on) — either via device testing or iOS Simulator
